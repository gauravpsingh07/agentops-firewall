# ---------------------------------------------------------------------------
# Frontend (.env.example) — placeholder values only.
# Angular reads runtime config from environment.ts; this file documents the
# values you may also want to expose to a build-time wrapper.
# ---------------------------------------------------------------------------

NG_APP_API_BASE_URL=http://localhost:8080/api
