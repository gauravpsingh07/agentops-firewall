# API reference (placeholder)

> This document will be fleshed out in Phase 8 (`docs: add API documentation`).

Planned sections:

- Authentication (JWT bearer for users, `X-Agent-Key` for agents)
- Endpoint catalog
- Request / response examples for each endpoint
- Error model
- Curl recipes for evaluators
